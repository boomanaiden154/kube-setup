apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: "vaultwarden-storage"
provisioner: linstor.csi.linbit.com
parameters:
  autoPlace: "2"
  storagePool: "ssd"
  resourceGroup: "vaultwarden-storage"
reclaimPolicy: Delete
